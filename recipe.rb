class Recipe

  attr_reader :name, :description, :rating
  def initialize(name, description, rating)
    @name = name # type more stuff
    @description = description # type more stuff
    @rating = rating # type more stuff
  end
end
